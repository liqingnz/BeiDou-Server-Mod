# 构建上下文排除项 —— 决定 `docker build` 要往 daemon 传多少东西。
# 没有这个文件的话，deploy/beidou-server-release（872 MB）、node_modules、.git
# 都会被打包上传，构建慢到没法用。
#
# ⚠️ 只排除“构建不需要的”。构建需要的有：
#   pom.xml, gms-server/pom.xml, gms-server/src,
#   gms-server/{wz,wz-zh-CN,scripts,scripts-zh-CN},
#   gms-ui/（package.json, yarn.lock, src, config, index.html, tsconfig 等）

.git
.gitignore

# IDE
.idea
gms-server/.idea
.vscode
*.iml

# 构建产物（镜像内会重新编译）
gms-server/target
gms-ui/dist
gms-ui/node_modules

# 运行期数据
gms-server/logs
deploy/beidou-server-release
deploy/docker-db-data
deploy/logs
deploy/.env

# 文档
docs
*.md
LICENSE
